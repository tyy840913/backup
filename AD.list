# DOMAIN: 匹配完整的域名（例如 www.google.com）
# DOMAIN-SUFFIX: 匹配域名后缀（例如 google.com 可以匹配 www.google.com和 mail.google.com）
# DOMAIN-KEYWORD: 匹配包含关键字的域名（例如 google 可以匹配任何包含"google"的域名）
# DOMAIN-REGEX: 使用正则表达式来匹配域名，最为灵活
# GEOSITE: 匹配预定义的域名集合（如 geosite:google 或 geosite:cn）
# IP-CIDR: 匹配 IPv4 地址或网段（例如 192.168.1.1 或 192.168.1.0/24）
# IP-CIDR6: 匹配 IPv6 地址或网段
# IP-ASN: 匹配属于特定自治系统（AS）的 IP 地址范围（通常用于识别网络运营商）
# IP-SUFFIX: 匹配 IP 地址的后缀（较为少见）
# GEOIP: 匹配目标 IP 的地理位置（例如 GEOIP,CN 匹配位于中国的 IP）
# SRC-GEOIP: 匹配源 IP 的地理位置
# SRC-IP-ASN: 匹配源 IP 所属的自治系统
# SRC-IP-CIDR: 匹配源 IP 地址或网段（例如 SRC-IP-CIDR,192.168.1.0/24）
# SRC-IP-SUFFIX: 匹配源 IP 地址的后缀
# DST-PORT: 匹配目标端口
# SRC-PORT: 匹配源端口
# IN-PORT: 匹配数据包进入的接口（入站端口）
# IN-TYPE: 匹配连接的类型（如 SOCKS5, HTTP）
# IN-USER: 匹配操作系统用户
# IN-NAME: 匹配入站连接的名称（在配置中定义）
# PROCESS-PATH: 匹配发起连接的进程的完整路径
# PROCESS-PATH-REGEX: 使用正则表达式匹配进程路径
# PROCESS-NAME: 匹配进程名
# PROCESS-NAME-REGEX: 使用正则表达式匹配进程名
# UID: 匹配用户 ID (Android 系统常用)
# NETWORK: 匹配网络类型（如 Wi-Fi, 蜂窝数据）
# DSCP: 匹配 IP 头中的差分服务代码点，用于 QoS
# RULE-SET: 引用另一组预定义的规则集
# AND: 逻辑"与"，组合多条规则，必须全部满足
# OR: 逻辑"或"，组合多条规则，满足一条即可
# NOT: 逻辑"非"，对一条规则取反
# SUB-RULE: 子规则（具体实现因工具而异）
# MATCH: 通常用作最终规则，匹配所有未被前面规则匹配的流量


# 番茄免费小说过虑广告
# 关键词
# DOMAIN-REGEX,^(toutiao|pangolin|zijieapi|douyin)(?=[.-])
# DOMAIN-REGEX,^[^.]*-?(toutiao|pangolin|zijieapi|douyin)-?[^.]*(?=[.-])
# 组1: zijieapi.com
DOMAIN-REGEX, ^(?:[a-z0-9-]+\.)*zijieapi\.com$|^(?:ad|vcs|ads[a-z0-9-]*-normal(?:-[a-z]+)?)\.zijieapi\.com$
# 组2: amemv.com
DOMAIN-REGEX, ^(?:[a-z0-9-]+\.)*amemv\.com$|^(?:ads[a-z0-9-]*-normal(?:-[a-z]+)?)\.amemv\.com$
# 组3: byteimg.com
DOMAIN-REGEX, ^(?:[a-z0-9-]+\.)*byteimg\.com$|^(?:p\d*-ad(?:-sign|-local-ads)?)\.byteimg\.com$
# 组4: pstatp.com
DOMAIN-REGEX, ^(?:[a-z0-9-]+\.)*pstatp\.com$|^(?:sf\d*-ttcdn-tos)\.pstatp\.com$
# 组5: zjurl.cn
DOMAIN-REGEX, ^(?:[a-z0-9-]+\.)*zjurl\.cn$|^(?:dig)\.zjurl\.cn$
# 组6: bdurl.net
DOMAIN-REGEX, ^(?:[a-z0-9-]+\.)*bdurl\.net$|^(?:dig)\.bdurl\.net$
# 组7: bsccdn.net
DOMAIN-REGEX, ^(?:[a-z0-9-]+\.)*bsccdn\.net$|^(?:bsccdn)\.net$
# 组8: bytesfield.com
DOMAIN-REGEX, ^(?:[a-z0-9-]+\.)*bytesfield\.com$|^(?:apps)\.bytesfield\.com$
# 组9: pangolin-sdk-toutiao.com
DOMAIN-REGEX, ^(?:[a-z0-9-]+\.)*pangolin-sdk-toutiao\.com$|^(?:pangolin-sdk-toutiao)\.com$
# 组10: pangolin-sdk-toutiao1.com
DOMAIN-REGEX, ^(?:[a-z0-9-]+\.)*pangolin-sdk-toutiao1\.com$|^(?:pangolin-sdk-toutiao1)\.com$
# 组11: pangolin-sdk-toutiao-b.com
DOMAIN-REGEX, ^(?:[a-z0-9-]+\.)*pangolin-sdk-toutiao-b\.com$|^(?:pangolin-sdk-toutiao-b)\.com$
# 组12: pglstatp-toutiao.com
DOMAIN-REGEX, ^(?:[a-z0-9-]+\.)*pglstatp-toutiao\.com$|^(?:pglstatp-toutiao)\.com$
# 组13: fqnovelvod.com
DOMAIN-REGEX, ^(?:[a-z0-9-]+\.)*fqnovelvod\.com$|^(?:v\d*-novelapp)\.fqnovelvod\.com$
# 组14: byteorge.com
DOMAIN-REGEX, ^(?:[a-z0-9-]+\.)*byteorge\.com$|^(?:lf\d*-orange)\.byteorge\.com$
# 组15: chengzijianzhan.cn
DOMAIN-REGEX, ^(?:[a-z0-9-]+\.)*chengzijianzhan\.cn$|^(?:localads)\.chengzijianzhan\.cn$



# 域名
DOMAIN,api.mixpanel.com
